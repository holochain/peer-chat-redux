{
  "name": "basic-chat",
  "version": "1.0.0",
  "description": "A chat demo for holochain",
  "files": [
    "./dna/",
    "./ui/"
  ],
  "scripts": {
    "hc:build": "mkdir -p dna && cd dna-src && RUST_BACKTRACE=1 hc package --strip-meta -o ../dna/holo-chat.hcpkg",
    "hc:start": "holochain -c ./conductor-config.toml",
    "hc:test": "cd dna-src && hc test",
    "prerelease": "cd ui-src && npm run clean && npm run build && cd .. && npm run hc:test && npm run hc:build",
  },
  "author": "",
  "license": "GPL-3",
  "devDependencies": {
    "gh-release": "^3.5.0"
  }
}
