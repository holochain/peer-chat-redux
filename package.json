{
  "name": "holochain-ui",
  "version": "1.0.0",
  "description": "A Holochain app (hApp) that you can add your own hApps to.",
  "scripts": {
    "hc:start": "mkdir -p tmp-storage && holochain -c ./conductor-config.toml",
    "hc:build": "rm -rf tmp-storage && mkdir -p dna && cd dna-src && hc package --output ../dna/holo-chat.hcpkg --strip-meta"
  },
  "repository": {
    "type": "git",
    "url": "git+https://willemolding@github.com/holochain/holochain-ui.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/holochain/holochain-ui/issues"
  },
  "homepage": "https://github.com/holochain/holochain-ui#readme",
  "dependencies": {
  }
}
